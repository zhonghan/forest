多线程的最佳实践：
1、给线程取一个有意义的名字
2、避免锁和缩小同步范围，比起同步方法，可能使用同步快。
3、都用同步类，少用wait notify， jdk提供了countDownLatch, Semaphore, CyclicBarrierhe 和Exchanger这些同步类简化了编码。
4、多用并发集合，少用同步集合。

多线程中忙循环就是程序员用循环让一个程序等待，不像方法wait，sleep， yield，它们都放弃了CPU控制。
而忙循环不会放弃CPU，他就是运行一个空循环。目的是为了保留CPU缓存，在多核系统中，一个等待线程醒来的时候可能会在另一个内核中运行，这样会重建缓存。